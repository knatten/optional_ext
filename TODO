Consider checking that we actually moved in the functions that are supposed to do so
Better names for the functions
Can we restrict transform_opt to only work on functions that return an optional? Probably, using traits and enable_if or something.
Demo of execute
